// Placeholder for domain modules (identity, tenants, authorization, menu,
// ordering, payments, ...). Modules are added per-ticket as the domain model
// is implemented — see docs/architecture/domain-boundaries.md.
export {};
